# AI Research Assistant

## Install the Necessary Packages

All the necessary Python pacakges are added to the ![Python requirements](/backend/services/requirements.txt) file. Run the below command to install all the packages from this file.

```bash
pip3 install -r requirements.txt
```